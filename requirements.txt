# Core
# NOTE: do not install openenv-core in Space root env:
# it requires websockets>=15 while Gradio 4.44.0 runtime pulls gradio-client (websockets<13).
# OpenEnv dependency remains declared in crucible_env/pyproject.toml for package builds.
pydantic>=2.0.0
huggingface_hub>=0.30,<1.0
anthropic>=0.40.0          # fallback if HF_TOKEN not set
groq>=0.11.0               # primary LLM backend

# Dashboard (must match HF Space sdk_version to avoid resolver conflicts)
gradio==4.44.0
# Keep ASGI stack aligned with Gradio 4.44 internals (newer FastAPI/Starlette break templates on Spaces)
fastapi==0.112.2
starlette==0.38.6
pillow==10.4.0             # avoid sdist fallback (pillow 10.3.0 fails on py3.13 builder)
plotly>=5.20.0
pandas>=2.0.0
matplotlib>=3.8.0

# Training tracking
wandb>=0.17.0

# RL training stack (install on GPU machine / HF Jobs)
# torch>=2.0.0
# transformers>=4.40.0
# trl>=0.8.0
# unsloth>=2024.4
# accelerate>=0.28.0
# peft>=0.10.0
# datasets>=2.18.0
# bitsandbytes>=0.43.0
